======================================================================
                     如何生成 SSH 密钥 
======================================================================

## [选项 1] Windows / macOS / Linux 终端 SSH 用户
----------------------------------------------------------------------
1. 打开系统终端。
   * Windows 系统请按下 "Win + X" 组合键，选择 PowerShell 或 "终端"
2. 在弹出的蓝色窗口中，粘贴并运行以下命令：
   ssh-keygen -t ed25519 ~/.ssh/id_<ID>.pub -C <my_server_key>
   * 请将 <ID> 换成便于记录的名称，如阿里云机器称为 'id_aliyun.pub'
   * <my_server_key> 可以改为加下了将在服务器创建的新用户名
   * 最终命令中不可出现任何特殊符号如 <>
3. 建议为密钥创建一个密码，并输入两次。密码将在 SSH 登录服务器时使用
   如果不需要密码，直接 Enter 回车两次即可
4. 运行以下命令查看并在屏幕上“选中并复制”公钥内容：
   cat ~/.ssh/id_<ID>.pub

## [选项 2] 使用旧版 Windows / PuTTY
----------------------------------------------------------------------
1. 打开 PuTTYgen 软件。
2. 点击 "Generate" 按钮，并在空白处移动鼠标生成随机数。
3. 复制上方 "Public key for pasting..." 下框中的所有内容。
   (以 ssh-rsa ... 开头)

======================================================================
获取到公钥内容后（以 ssh-xxxx 开头的一长串字符），
请运行 sudo bash init2.sh 并将其粘贴进去
======================================================================